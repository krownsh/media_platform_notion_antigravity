## Obsidian Vault 路徑回復（未部署）

- 依 Owner 決定，停止 `wiki/sources`、Collection index 與 DB-only manifest 的 Obsidian 改造；沒有連線、掃描或修改真實 Vault，沒有修改 Supabase。
- Vault writer 回復既有 `wiki/threads/<platform>/<日期>-<標題>--<post-id前8碼>.md`；Collection、Topic、AI domain、Project 建議與貼文移動不再驅動 Vault 路徑或建立 Vault index。
- 保留 AI 標題作為無來源標題時的檔名 fallback，並保留復刻方案的 workflow 資料、前端 badge／詳細呈現與同一來源筆記區塊。
- 移除未部署的 Vault content migration／DB-only manifest 腳本、npm 指令與測試；auto-container dry-run 的建議路徑同步改回 platform 規則。
- 驗證：針對性 14 個 Node tests、完整 171 個 Node tests、`npm run lint`、`npm run build` 都通過；Docker 依 Owner 指示未執行。build 僅有既有 Browserslist 與大於 500 kB bundle 警告。

## 合併貼文流程與後續行動 badge

- 主 task 完成後，將 `codex/workflow-badges` 的 `c25c528` 合併至 `main`；唯一衝突為同日工作日誌的同時追加，雙方內容皆保留。
- 主流程 badge 維持右上角文字狀態；研究、POC 提案、POC 執行、復刻方案、快速改寫、內容整合維持為可並存的後續行動 badge。
- 合併後驗證：3 個針對性 Node tests、`npm run lint`、`npm run build` 均通過；未 push、未部署、未異動資料庫或 Vault。

## 全系統唯讀稽核（Terra）

- 依 `docs/system_audit_plan_2026-09-08.md` 盤點 Git、文件、工作流程、schema 契約與安全測試；未改程式／tasks.md，未連 Supabase／Vault，未跑 Docker、爬蟲、付費 API 或部署。
- 產出 `docs/system_audit_report_2026-09-08.md` 與 `docs/worktree_disposition_2026-09-08.md`；列出 11 個 worktree、無 stash、local main 比遠端 tracking/HEAD `6dd66b2` 領先 16 commits。
- 已通報重大風險：追蹤的 archive 設定檔含未遮蔽 MiniMax API key；另記錄 Vault 重試路徑可能因 AI title／日期變動另建筆記、tasks 狀態矛盾、舊 Vault move proposal 殘留與 API-first 文件／crawler-only runtime 不一致。
- 本輪安全檢查：6 個隔離 Node tests 與 `npm run lint` 通過；跳過 build（會覆寫 ignored `dist/`）及所有會讀取正式憑證、DB、Vault 或 Docker 的測試／命令。

## 盤點後第一批本機修正（未部署）

- 從目前 Git 版本刪除 `server/archive/legacy-config/temp_env.txt` 的暴露設定檔，並加入精確 ignore；歷史 commit 仍可能含秘密，需在供應商撤銷憑證後另行規劃歷史清除。
- Vault writer 現在優先驗證 workflow 已記錄的 Vault 相對路徑；標題或日期變動時沿用同一份屬於該 post 的筆記。已記錄檔案缺失、超出 `wiki/` 或屬於其他 post 時會明確失敗，不另寫第二份。
- auto-container 唯讀 planner 停止產生 `vault-plan.csv` 或 Vault move 建議，保留 Collection／Topic 盤點輸出。
- 驗證：12 個相關 Node tests 與 `npm run lint` 通過；未讀取 Vault／Supabase 正式資料、未跑 Docker、未撤銷遠端憑證、未 push 或部署。

## 任務狀態校正

- 將根目錄 `tasks.md` 的 Obsidian 回復任務同步為已完成，記錄其已合併至本機 `main`、未 push／未部署及未操作實際 Vault 的事實；不移除使用者原始任務內容。

## 擷取路由文件校正

- 依 runtime 實作與既有 crawler-only 決策，追加 `AGENTS.md` 補充：官方 API agent 僅為未接線預留設計，維運與文件不得假設 API-first fallback 已存在；未修改 crawler、API、資料庫或部署。

## MiniMax 退役決策

- Owner 確認 MiniMax 已無訂閱；不安排撤銷或 Git 歷史重寫。後續不接入新付費模型，並把既有 server LLM 入口改為可見的 Hermes agent 交接狀態；Hermes/Codex 不被誤當成可由 server 直接呼叫的 provider。
- 實作：移除 `aiService` 的 MiniMax HTTP client 與 key 讀取；分析、改寫與 Remix API 統一回傳 `503 HERMES_AGENT_REQUIRED`，Remix UI 改為顯示 Hermes Codex agent。擷取與 deterministic 分類保留，背景分析失敗維持 pending。
- 驗證：AI provider／capture analysis 針對性測試、完整 180 個 Node tests、`npm run lint` 與 `npm run build` 通過；Docker 未執行。build 只有既有的 Browserslist 資料過期與 bundle 大於 500 kB 警告。

## Vault retry 與 artifact 保存補證

- Vault writer 新增已記錄 note／draft 的實體路徑解析檢查：符號連結越界、其他貼文或人工 note、遺失 draft 都明確拒絕；只有已驗證、同 post 且受 managed block 保護的路徑才會沿用。草稿改標題重試仍保留原檔與人工補充。
- `agent-dev` 的五份未追蹤 container preflight artifact 已建立路徑、大小、SHA-256 與用途清冊，保留在原 worktree；`vault-plan.csv` 明確標記不可執行。
- 更新目前有效的 AI／POC／Content Studio 文件與 `server/.env.example`：server 無 MiniMax provider，Hermes Codex agent 是外部工作流；歷史稽核報告保持原始觀測，不被改寫。

## Supabase 唯讀補證

- 使用 Supabase connector 只讀確認 project ref、migration、目標表 RLS 與 policy metadata；未讀取貼文內容、未執行 DDL/DML、未部署。
- 確認 Stage O/P/Q/R/S migration 已在 `dcyjictvatixbflfrsfg`；`collection_posts` 與 `collection_collections` 已是 authenticated owner-only RLS。另發現 collection map、analysis、comments 三張附屬表仍用舊 `TO public` policy，雖有 `auth.uid() = user_id` 條件而不會讓 anon 讀到資料，但需獨立確認是否納入下一個 RLS hardening scope。
- GitHub 遠端 `main` 仍為 `6dd66b2`；本機修正未 push，無法推論 server/Hermes 已部署。未取得主機 connector，因此實際服務與排程 revision 記為未確認。

## 資料庫身分更正

- 更正先前口頭與文件的錯誤表述：`dcyjictvatixbflfrsfg` 是本專案專用資料庫，含 `collection_posts` 等本專案資料；`nfnjxjmarzevkxawkzro` 才是共用資料庫。此次 Supabase connector 僅對 `dcyjictvatixbflfrsfg` 執行唯讀 metadata 查詢，未對任何資料庫執行 DDL、DML、migration 或部署。

## main 推送

- 經 Owner 明確授權，已將本機 `main` 的 25 個既有 commits 推送至 `origin/main`：`6dd66b2` → `2aa56c2`。未部署服務、未修改 Supabase、未操作 Vault。
