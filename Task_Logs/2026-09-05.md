# 2026-09-05

## 停止自動容器增生：驗收與 dry-run

- 以明確 `--env-file` 載入同專案服務憑證，對 Supabase 執行 read-only
  audit；沒有執行 INSERT、UPDATE、DELETE、DDL，沒有讀寫 Vault。
- 產生 `artifacts/container-migration/2026-09-05-stage-o-stage-p-preflight/`
  dry-run manifest：baseline、Collection／Topic／Vault plan 與 unresolved。
  每一筆都有舊 ID 或路徑、post ID、建議目標或 action、依據、confidence
  與 `requires_owner_confirmation`。
- 現況為 377 Posts、55 Collections、60 Topics、33 個 Hermes auto
  Collections、60 個 `agent_auto` Topics；跨 tenant Collection link 為 0。
  所有 dry-run row 均為 confidence 0，沒有自動遷移。
- 發現另一 session 的 Topic governance 與本 session 在
  `autonomousKnowledgeService` 有實際重疊。整合後只允許連結
  `origin=user` 且 `active` 的 Topic，match 一律先為 `suggested`；legacy
  `agent_auto` Topic 不得接收新 source match，拒絕時仍保留 suggestion。
- 針對性測試、ESLint、`git diff --check` 通過；Vite production build
  exit code 0。Docker 驗證依 Owner 已明示決定跳過。
- Stage P RLS 仍未部署；需要付費 Supabase branch 的雙帳號驗證與 Owner
  成本確認後才可進入線上 migration。

## Stage P 直接部署（Owner 授權）

- Owner 決定不建立付費 Supabase branch，明確授權直接上線。套用 migration
  `stage_p_collection_rls_hardening`，Supabase 記錄版本為
  `20260904175657`。
- 部署前確認 `collection_posts` 377 筆、`collection_collections` 55 筆，
  RLS 尚未啟用，same-owner key/FK 尚不存在，且跨 tenant 或遺失
  Collection link 為 0；migration 的 transaction preflight 也會重複阻擋
  此類異常。
- 部署後兩表 RLS 均啟用，各有 4 個 `authenticated` policy；`anon` 對兩表
  沒有 SELECT grant，`authenticated` 與 `service_role` 保有完整 CRUD。
  `(id, user_id)` unique key 與 `(collection_id, user_id)` same-owner FK
  已存在，資料計數維持 377／55、cross-tenant link 為 0。
- 使用既有 owner JWT claim 的 transaction-local authenticated read 模擬，
  仍可讀取 377 Posts／55 Collections；交易已 rollback，沒有寫入資料。
  Security Advisor 對這兩張表的「policy exists, RLS disabled」錯誤已消失。

## 兩個 Session 合併前盤點

- 唯讀確認目前 `main` 仍在 `75a38da`，另一 Session 的 UI 與 Topic governance
  成果直接留在 root working tree、尚未 commit；本任務則位於獨立
  `agent-dev` worktree，HEAD 為 `4891248`。
- 找到 7 個雙方共同修改的檔案，其中 5 個已由 patch preflight 證實會直接
  衝突；禁止直接 merge 到 dirty main。
- 發現 DB-only preprocess 的已部署 Stage O 函式仍可把高分 Topic match
  標成 `accepted`。另一 Session 的 Node wrapper 雖已擋住 topic payload，
  資料庫本身仍應用後續 additive migration fail-closed。
- 新增 `.hermes/plans/2026-09-05-two-session-main-integration.md`，定義先把另一
  Session 收斂至獨立分支，再以 integration worktree 人工合併、跑完整測試，
  最後才合回 main。此步未修改 main、未 push、未遷移舊資料。

## 兩個 Session 隔離整合

- 將 root working tree 的另一 Session 成果收斂至
  `codex/ui-topic-governance` commit `805e4d5`；原 `main` commit 未改動。
- 建立 `codex/integrate-september` worktree，先合併 UI／Topic branch，再合併
  `agent-dev`。四個實際衝突均以人工逐段整併，沒有整檔選擇 ours/theirs。
- `preprocess-workflow.js` 同時保留固定 Vault／link-only 分類與 accepted Topic
  POC gate；`rootSaga.js` 同時保留 `takeLeading` 請求去重與 RLS owner filters。
- 新增尚未部署的 Stage Q deployment source 與契約測試，補上資料庫層 Topic
  match 防護：agent 只能對 active user Topic 建立 `suggested` match，人工決定
  才能成為 `accepted`／`rejected`。
- 整合測試第一次為 39/40，揭露 remote wrapper 仍檢查舊的 `topic.title`，導致
  v2 `suggested_title` proposal 遺失；已修正並保留稽核 proposal，第二次及
  最終測試為 40/40。
- 完整 `npm run lint` 最初發現 Vault path builder 的未使用相容參數；改名為
  `_options` 後完整 lint 通過。最終 `npm run build` 通過（僅有既有
  Browserslist／bundle size 警告）。
- Supabase 唯讀 postflight：Stage O ×2 與 Stage P migration history 均存在；
  Posts／Collections／Topics／matches／Projects 為 377／55／60／60／0，兩個
  Collection 表 RLS 已啟用、agent_auto Topic blocker 存在、跨 owner link 為
  0。Stage Q trigger 尚不存在，符合「只準備、未部署」狀態。
- integration merge commit 為 `0acd89c`。另補跑 Node 全專案自動探索測試，
  156/156 全數通過；Docker 依 Owner 指示未執行。

## 合併回 main

- Owner 明確授權後，將 `codex/integrate-september` 以 `--ff-only` 合併回
  本機 `main`；`main` 從 `75a38da` 前進至已驗證整合成果 `44d17aa`，過程
  沒有衝突。
- 未 push、未部署 Stage Q、未搬遷或刪除既有資料與 Vault 檔案。

## Stage Q 直接部署（Owner 授權）

- Owner 明確授權套用後，將
  `database/deployments/stage_q_topic_match_governance_hardening.sql` 套用至
  Supabase project `dcyjictvatixbflfrsfg`；migration 版本為
  `20260904190651`。
- 部署後確認 trigger `enforce_agent_topic_match_review` 已啟用；trigger
  function 採 `SECURITY INVOKER`、固定 `search_path=public, pg_temp`，直接
  EXECUTE 權限僅保留給 `postgres` 與 `service_role`。
- 交易式 smoke test 確認 agent 寫入會被強制收斂為
  `suggested`／`agent`、使用者決定仍可維持 `accepted`／`user`，且 legacy
  `agent_auto` Topic 會被拒絕。測試交易已 rollback，沒有留下測試資料。
- 線上資料維持 60 Topics、60 matches（35 accepted、25 suggested），與部署
  前完全一致；沒有搬遷、刪除、合併或重分類任何既有資料。
- Supabase Security Advisor 共回報 213 項既有 lint；Stage Q function、trigger
  與兩張相關 Topic 表沒有新增 lint。未 push 遠端。
