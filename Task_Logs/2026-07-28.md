# 2026-07-28 開發日誌

## 任務：接續 POC 生成與沙盒執行

### 起始盤點

- 使用者指定專案模式為 `sideproject`。
- Git 位於 `docs/agent-codex-action-vault-v2-20260724`，起始工作樹乾淨。
- 2026-07-24 已完成 Route Agent、Enrichment、Project Auditor、Opportunity Matcher 與初版 Docker 檔；POC 生成、執行與結果寫回尚未完成。
- `C:\codex_home\core\path-contract.md` 缺失，全域規則投影不完整，因此未修改全域 Kanban。

### 本次完成

1. 新增 `server/services/pocService.js`：
   - MiniMax 結構化 POC 生成。
   - JavaScript／Python 語言正規化。
   - 靜態拒絕網路、env、秘密、檔案、子程序、shell 與動態執行。
   - 唯一 run directory、manifest、SHA-256 與 bounded output。
   - Windows 直接執行 Docker Compose；Mac/Linux 使用 `runner.sh`。
   - success/error/timeout/output-limit 結果寫回 `collection_post_analysis.insights` JSONB。
2. 強化 `sandbox/docker-compose.yml`：斷網、唯讀、tmpfs、cap drop、no-new-privileges、CPU／memory／PID 限制，並分開 Node 20 與 Python 3.12 runner。
3. 更新 `analyze-item.js`：最高分 application case 預設自動生成／執行 POC；支援 `--propose-only`；所有失敗以非零 CLI 結束。
4. 修復既有缺陷：
   - `complete-item.js` 改用 Stage B 已定義的 `sent`、`last_error`、`locked_at`、`locked_by` 與 `payload`。
   - `server/index.js` 補回 bearer token regex 的 `match` 宣告。
   - 移除未安裝的 `node-fetch`，改用 Node 20 原生 `fetch`。
   - MiniMax／Tavily HTTP 加入 timeout；MiniMax structured JSON 支援 Markdown fence 清理。
5. 新增 `test/script/poc_service.test.js` 與 outbox／auth 契約 assertions。
6. 新增 `docs/poc_sandbox_workflow.md`，包含 Windows／Mac 設定、執行方式、安全邊界、JSONB 契約與限制。

### 驗證結果

- Node 語法檢查：通過。
- POC 單元測試：5/5 通過。
- 全部 Node 測試：24/24 通過。
- 修改範圍 ESLint：通過。
- `docker compose config --quiet`：通過；Docker config 權限警告不影響 Compose YAML 驗證。
- Supabase POC storage 唯讀 preflight：通過，`collection_post_analysis.insights` 可存取，回報 288 rows；未讀出貼文內容、未寫入資料。
- `npm run build`：通過，2383 modules transformed；保留既有 chunk > 500 kB 與 browserslist 資料過舊警告。

### 未完成／阻擋

- 本輪經授權啟動 Docker Desktop，daemon 27.1.1 可用且沒有執行中容器；`node:20-alpine`、`python:3.12-alpine` 都不存在。
- Docker Desktop VHDX 實際位於 `C:\Users\bgkon\AppData\Local\Docker\wsl\disk\docker_data.vhdx`（約 35.98 GB），因此遵守大型快取不得寫入 C 槽的規則，沒有 pull runner images，也沒有執行真實 Node／Python container smoke test。檢查後已關閉 Docker Desktop。
- 尚未用真實 outbox item 呼叫 MiniMax／Tavily，也未執行遠端 Supabase 實際 JSONB 寫入，避免在容器環境未完整前浪費額度或產生半套資料。
- Stage C migration 註解提到 `experiments`、`experiment_runs`、`integration_proposals`，但 SQL 尚未建立；本輪遵守不改正式 Schema，沒有補建。
