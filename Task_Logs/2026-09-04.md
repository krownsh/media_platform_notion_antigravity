# 2026-09-04

## 前端貼文請求與卡片呈現修正

- 模式：sideproject（使用者於本次對話明確確認）。
- 移除 `App.jsx` 的 15 秒 `/api/posts` 與 capture history 輪詢；Hermes 維持外部 Cron Pull，前端不再以輪詢驅動畫面更新。
- 將首次貼文載入集中於 `App.jsx`，移除首頁、所有貼文與貼文詳情的重複 fetch。
- 在 Redux 加入 `refreshing`，背景讀取不再把已載入的列表切成 skeleton；Saga 對 `/api/posts` 改為 `takeLeading`，阻擋重疊請求。
- 貼文卡片在沒有圖片 URL 時不再顯示空白圖片區或提示文字。
- 所有貼文分類篩選補齊 `productivity`、`design`、`crypto`。
- 新增 `docs/frontend/data_loading.md`，記錄貼文載入與卡片媒體規則。
- 驗證：`npx eslint src` 與 `npm run build` 通過。完整 `npm run lint` 會掃描既有未追蹤的 `.worktrees/agent-dev`，並被其中舊版 `server/archive/crawler-prototypes/threadsCrawler.launcher.js:66` 的解析錯誤阻擋；本次沒有修改該 worktree。

## 主題工作區資料盤點與治理規劃

- 使用者指定主題採「專案為主、領域為輔」，並要求將接受貼文匹配與以主題驅動研究／POC 納入規劃，同時涵蓋 Hermes 腳本。
- 唯讀查詢 Supabase：`collection_topics` 有 60 筆、1 位 owner，所有 topic 均為 `agent_auto`；`collection_topic_source_matches` 有 35 筆 agent 自動 accepted、25 筆 suggested。
- 根因已定位為後續 Stage K／M 與 `scripts/agent-sdk/preprocess-workflow.js` 的自動 topic／match 寫入，與早期 MVP 的人工確認設計衝突。
- 尚未修改或封存任何既有主題／匹配，等待使用者確認新的專案樹與領域字典。

## Active project registry

- 使用者採用「專案為主、領域為輔」的 A 方案，並要求納入其 GitHub 近 30 天有更新的 repo。
- GitHub 查詢 `user:krownsh pushed:>=2026-08-05` 確認六個 active project：`media_platform_notion_antigravity`、`my_hater_react_native`、`my_full-stack-path-inspector`、`my-chrome-extension-ordering`、`v0-stock-portfolio-dashboard`、`My-sticker-book`。
- 已建立 `docs/topic_workspace_governance_plan.md`，定義 project registry、受控領域、既有資料的非破壞性遷移，以及 Hermes／Codex preprocess、research、POC、decision scripts 的對齊範圍。

## 主題治理與 Hermes 人工關卡實作

- 新增尚未套用的 `database/deployments/stage_o_topic_project_governance.sql`：建立 `collection_projects`、以 `project_id + domain_key` 管理 topic、記錄 match 的人工決定來源，並以 trigger 阻擋新的 `agent_auto` topic。此 SQL 不會修改或刪除現有 60 筆主題。
- 新增專案 registry API、專案 × 領域 topic API、貼文匹配接受／略過 API，以及 Topic UI 的六個 active GitHub repo 快速加入按鈕。
- Hermes preprocess 不再建立 topic；模型 topic 僅以 proposal 留在 workflow context。既有 user-owned active topic 只會得到 `suggested` match，絕不自動 accepted。
- Codex remote preprocess 在送入既有 Stage M RPC 前清除 `topic` / `relation` 寫入輸入，並保留 `topic_proposal` 稽核資料。
- research、preprocess 內建 POC 與顯式 POC 執行均要求 source 具有已接受、user-owned、active topic match。
- 驗證：`node --test test/script/topic_governance.test.js`、目標檔案 ESLint、`npm run build` 全部通過。

## Stage O Supabase 部署

- 使用者明確授權後，已在 Supabase project `dcyjictvatixbflfrsfg` 套用 migration `stage_o_topic_project_governance`。
- Smoke test：`collection_projects` 已建立並啟用 RLS／owner policy；topic 新欄位 `project_id`、`domain_key` 和 match 新欄位 `decision_source` 已存在；`agent_auto` 防呆 trigger 已驗證會阻擋寫入且沒有留下 smoke row。
- 資料核對：部署前後 `collection_topics` 均為 60 筆、`collection_topic_source_matches` 均為 60 筆，`collection_projects` 為 0；本次未改寫既有資料。
- Security Advisor 顯示既有 `collection_posts`、`collection_collections` 的 policy/RLS 問題及其他 legacy schema advisory；不屬於 Stage O 範圍，未在本次未授權的情況下修改。
