# 2026-09-07

## 內容整理第 3 階段第 4 批完成

- 套用剩餘已審閱的 14 筆 inbox 分類：副業 4、prompt 4、帳號監控 1、agent loop 1、ai漫劇 1、ai圖片+影片 3。
- 初次唯讀 preflight 因 prompt Collection UUID 少一個字元而被資料庫拒絕；SQL 未執行、沒有資料寫入。修正 UUID 後，重新確認 14/14 仍在 inbox、同一 Owner、目標 Collection 與搜尋投影均完整。
- 使用單一 JSON move mapping 的原子交易，同步更新 `collection_posts.collection_id` 與 `collection_post_search_documents.collection_id`；交易內要求兩者各自更新 14 列，任一數量不符即 rollback。
- Postflight：14/14 貼文與搜尋投影皆在指定目標；總貼文仍 378、inbox 56→42、全庫搜尋投影不一致 0。42 篇 inbox 因低信心／歧義／沒有適合既有內容分類而保留，沒有硬塞分類。
- 批次 journal：`docs/content_organization_batch_04_2026-09-07.md`。未變更 Topic、Project、scope、workflow JSON、Vault、排程或部署。

## Vault 舊路徑遷移器

- 新增 `scripts/maintenance/migrate-vault-content-paths.js`，只遷移 `wiki/domains/` 的既有來源筆記至目前 Collection 或 inbox；採 manifest-first、hash 與 workflow `updated_at` 驗證，並在 DB 成功更新後才刪除舊檔。
- 本機 Vault 健檢確認 Windows 沒有真實 Vault，因此未執行實體搬移；Mac 指令與限制記錄於 `docs/vault_content_migration.md`。

## 收藏庫介面清理與 AI 標題規劃

- 建立 `codex/library-ui-cleanup` 隔離 worktree，從最新 `main` 的 `6dd66b2`
  開始，避免與同時進行的 Vault 工作交錯。
- 前端將 Hermes 自動建立的歷史 Collection 從首頁、sidebar、移至資料夾選單與
  主題範圍設定統一排除；沒有刪除資料庫中的 33 個空歷史 Collection。
- 改善卡片常態框線與 hover 強調、詳細頁原始貼文連結、Collection modal 遮罩關閉、
  modal 置中卡片排列、明確工作流／POC 狀態與搜尋語意。
- 新增 `.hermes/plans/2026-09-07-ai-title-backfill-and-workflow.md`；規劃保留
  來源 title、將 AI title 存在 analysis、先做 dry-run 再經 Owner 授權回補，尚未
  呼叫模型、遷移 schema 或更新任何正式貼文。
- 修正未來 URL capture 的摘要完整性：模型未回傳可用 summary 時不再標示 analysis
  完成，而是保留待重試狀態；既有 51 篇缺摘要貼文未被修改。
- 驗證：collection visibility 單元測試通過、`npm run lint` 通過、`npm run build`
  通過；Docker 依 Owner 指示未執行。

## AI 標題第一階段完成（未部署）

- 新增未來流程的 AI 顯示標題 contract：只在來源沒有 title 時，才將模型同次回覆的
  `generated_title` 寫入 analysis；來源 title 不覆寫。
- 已套用 Stage S additive deployment：新增三個 analysis 欄位與來源標題保護 trigger；
  沒有回填、刪除、移動或更新既有貼文，沒有呼叫模型。
- Production postflight：貼文數維持 383、三欄皆存在、trigger 存在、
  `collection_post_analysis` 的 RLS 維持啟用；trigger function 為 security invoker，
  anon/authenticated 都無直接 execute 權限。
- 舊貼文回補 dry-run 已完成：375 篇候選，估計輸入 18,081 tokens、輸出 15,000
  tokens。未呼叫模型、未寫入資料；manifest 位於本機隔離 artifact 目錄，待 Owner
  明確授權才建立與執行實際批次回補。
- 驗證：34 個相關 Node tests、`npm run lint`、`npm run build` 均通過；Docker 依 Owner
  指示未執行。build 僅保留既有 Browserslist 與大於 500 kB bundle 警告。

## AI 標題既有資料回補完成

- 依 Owner 授權，使用 Codex Luna subagent 回補既有無來源標題貼文；未使用已廢棄的 MiniMax。
- Production postflight：`remaining_candidates = 0`、`codex_luna_backfill = 374`、
  `untitled_posts_with_generated_title = 374`、join 貼文 381。
- 失敗 0 篇；僅更新 `collection_post_analysis.generated_title`、`title_generated_at`、
  `title_generation_source` 三欄，未修改 `collection_posts.title`、summary、tags、topics 或其他欄位。
- 未執行 Docker、部署、push 或 merge；資料庫回補已直接完成於目前 production project。

## Vault 穩定來源路徑與復刻方案呈現

- 依 Owner 確認，來源筆記不再使用平台、Collection 名稱、AI domain 或標題作實體路徑；新寫入固定為 `wiki/sources/<post-id>.md`。
- Collection 改為 `wiki/collections/<collection-id>.md` 索引，未分類使用 `wiki/inbox.md`；Collection 改名與貼文移動只更新索引與 managed metadata，不移動來源筆記。
- 內容草稿也改為 post ID 固定目錄；復刻方案仍留在同一份來源筆記，並在貼文卡片顯示狀態 badge、詳細頁顯示名稱、目標、MVP、驗收條件與既有 action 狀態。
- 更新 action-plan 正規化，保留 `replication_plan` 的 goal、MVP 與 acceptance criteria；沒有新增資料表、沒有自動建立 Project。
- Vault 遷移器改為將任何既有 `wiki/` 相對路徑 manifest 至 `wiki/sources/<post-id>.md`，但未執行真實 Vault 盤點或搬移。
- 驗證：17 個針對性 Node tests、`npm run lint`、`npm run build` 通過；Docker 依 Owner 指示未執行。build 僅保留既有 Browserslist 與大於 500 kB bundle 警告。

## Vault DB-only 路徑盤點

- 修正 Vault 遷移流程：先不需要 Vault 根目錄的 `--database-only` 唯讀 manifest，後續才到實體 Vault 驗證 hash／衝突。
- production DB-only postflight：384 個 workflow；337 筆 `ready_for_filesystem_verification`，規劃由既有 `wiki/` relative path 對應至 `wiki/sources/<post-id>.md`；47 筆因沒有 workflow Vault path 而跳過。沒有資料庫更新、沒有 Vault 檔案操作。
- 第一次查詢因已不需要的 Collection relation join 有多重關聯而被 Supabase 拒絕，未寫入；移除該 join 後重跑成功。
- 實際套用仍固定採 copy → workflow compare-and-update → 移除舊檔。不得先改 DB，避免遠端路徑先指向不存在的筆記。
- 第二次 production DB-only manifest 加入共用路徑保護後：334 筆可進入實體檔案驗證、47 筆沒有 workflow 路徑而跳過、3 筆共用 `wiki/entities/qiaomu-seo.md` 而 blocked；目標路徑碰撞 0。共用 entity note 不會被自動複製成三篇來源筆記。
