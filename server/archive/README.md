# Archived server material

This directory preserves historical debugging scripts and crawler prototypes
without leaving them on the runtime path. Several files target the retired
`posts` / `post_analysis` schema or depend on one-off local data.

They are retained only as investigation history. Do not execute them against
staging or production, and do not treat archived configuration as a source of
credentials.
