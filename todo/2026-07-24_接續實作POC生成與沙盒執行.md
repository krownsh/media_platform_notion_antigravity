# 待辦事項：接續實作 POC 生成與沙盒執行
**日期**：2026-07-24 (準備於明天繼續)

## 目前進度摘要
- [x] **架構升級**：完成 `master_plan.md` 的設計更新。
- [x] **Enrichment Phase (擴充檢索)**：完成 `enrichmentService.js`，並加入本地快取防止浪費 Tavily 額度。
- [x] **Sandbox Isolation (沙盒隔離)**：建立 `sandbox/` 資料夾與 `docker-compose.yml`、`runner.sh`。
- [x] **SDK 整合**：`analyze-item.js` 已成功串接 Phase 1.5，能正確撈取外部知識。

---

## 明日任務順序 (Next Steps)

### 1. 實作 POC 代碼生成 (POC Generator) — [x] 程式完成
- **目標**：在 `analyze-item.js` 的 Opportunity Matcher 判斷需要執行 POC 測試後，觸發 LLM (Claude/MiniMax) 自動撰寫一段 `.js` 或 `.py` 的測試腳本。
- **輸入**：貼文原始內容 + Phase 1.5 取得的官方文件擴充知識。
- **輸出**：一段可執行的程式碼字串。
- **2026-07-28**：新增 `pocService.js`，沿用 MiniMax provider，支援 JavaScript／Python，包含 schema 驗證與危險程式碼靜態拒絕。

### 2. 串接沙盒自動化執行 (Sandbox Automation) — [x] 完成
- **目標**：將上一步生成的程式碼，直接存成 `sandbox/test_script.js`。
- **執行**：在 Node 裡面透過 `child_process.exec` 自動呼叫 `bash sandbox/runner.sh test_script.js`。
- **擷取**：捕捉 stdout / stderr，判斷 POC 執行結果是否成功。
- **2026-07-28**：Windows 改由 Node 直接執行 `docker compose`，Mac/Linux 保留 `runner.sh`；容器已限制網路、寫入、權限、CPU、記憶體、process 與 timeout。
- **2026-07-29**：Docker VHDX 已透過官方 GUI 搬至 G 槽；Node／Python runner images 已下載，JavaScript 與 Python 真實 container smoke test 均成功。
- **2026-07-29**：第 5 筆 outbox 已完成 MiniMax 路由、需求媒合、受限 Docker 與 Supabase JSONB 寫回；安全 fallback 成功，outbox 仍待其他路由處理。

### 3. 將 POC 結果寫回資料庫 (Store Results) — [x] 完成
- **目標**：把 POC 產生的程式碼與終端機輸出結果 (Success/Error 訊息) 寫回 Supabase 的 `collection_post_analysis` 或是新建立一個 `project_poc_results` table（依循「不改正式 Schema」的原則，優先存在 JSONB 欄位內）。
- **2026-07-28**：不改 Schema，先追加到 `collection_post_analysis.insights` JSONB；成功與失敗皆保存，DB 寫回失敗會讓 CLI 非零結束。遠端唯讀 head/count 已確認 `insights` 欄位可存取（288 rows），尚未製造測試寫入污染 production 資料。
- **2026-07-29**：真實 POC run `66673c21-fd9f-423a-a8ad-d7bed8420d9a` 已寫回 Supabase；包含程式碼、雜湊、stdout、耗時、成功狀態與 fallback 原因。

### 4. 驗證完整 End-to-End 工作流 — [x] 完成
- **操作**：使用真實貼文 (如 HUMAN.md)，讓 Agent 走完「讀取 -> 檢索擴充 -> 寫程式 -> Sandbox 執行 -> 記錄結果」的完整流程。
- **期待**：能看到終端機印出 Docker 內的成功執行結果。
- **2026-07-29**：已使用真實 outbox 完成 MiniMax／Tavily／Docker／Supabase E2E，輸出 `POC passed: detected 2 deterministic findings`；貼文詳情頁已可顯示 POC 驗證結果。

### 5. GitHub App 唯讀 repo 目錄同步 — [ ] 暫停

- **目標**：讓產品端以 GitHub App 的只讀安裝權限同步使用者授權的 repo，建立可選的專案目錄；GitHub `owner/repository` 作為跨電腦穩定識別，本機 checkout 只在明確 POC 時使用。
- **已完成**：建立 GitHub App、確認 Codex GitHub connector 可唯讀列出 `krownsh` 的 11 個可存取 repo、建立私鑰忽略路徑與設定文件。
- **暫停點**：尚未產生 App private key、尚未安裝 App 到帳號的所有 repo、尚未填入後端環境變數；因此沒有產品端 GitHub API 呼叫、沒有 clone、沒有 repo 資料寫入資料庫。
- **恢復順序**：產生並本機保存 PEM → 安裝 App（All repositories）→ 設定後端 App ID / 私鑰路徑 → 驗證短效 installation token → 實作 repo 目錄同步與 UI 選擇。

### 6. 未整理貼文資料夾建議 — [x] 完成

- **目標**：未分類來源能看見建議資料夾，但沒有使用者點擊前不可移動或改寫收藏資料。
- **2026-07-29**：已在貼文卡片接上 deterministic 建議與「建議移至」動作；實際 Code Review 來源首選 `工程師開發優化`。規則不呼叫模型或外部 API。
- **2026-07-29**：已將該 Code Review 原文移入 `工程師開發優化`，並綁定本專案的 GitHub target；只提案驗證成功重用既有 POC 結果，未執行外部 POC。
