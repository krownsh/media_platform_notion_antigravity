interface:
  display_name: "MediaCrawl Inbox"
  short_description: "Route captured URLs and analyze private images"
  default_prompt: "Use $my-mediacrawl-skill to process the oldest available media capture safely."
