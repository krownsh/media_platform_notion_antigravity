# 2026-09-04

## 停止一篇貼文一個資料夾／Topic

- 在隔離 `agent-dev` worktree 實作 Task 1–7 的程式與文件部分；未修改既有資料、Vault、排程或正式資料庫。
- Local preprocess 改為只能連結同一 user 的既有 `collection_id`／`topic_id` 或既有 slug；自由文字分類寫入 `workflow.context.classification_suggestions`。
- Remote DB-only preprocess 新增 Stage O SQL，取代自動建立 Collection／active Topic 的路徑。
- Vault 來源筆記固定為 `wiki/threads/<platform>/<date>-<title>--<post-id>.md`；復刻方案併入同一筆記的 managed block。
- 新增 read-only audit 與 migration-plan 腳本；未執行，因目前環境未提供可用 Supabase credentials，且不應在未確認輸出位置時產生 live dry-run。
- Docker 規定測試曾因 Docker Desktop Linux engine 未啟動而無法執行；Owner 已於 2026-09-04 明確決定跳過 Docker 驗證。

## Stage O 流程保真修正

- 重新核對後發現早期 Stage O 縮寫版遺漏 Stage M 的分析、重複貼文判定、autonomy policy、Vault sync、review request、action plan 與權限邊界；未部署該版本。
- Stage O 現已重建為 Stage M 的完整流程等價版本，只移除自由文字建立 Collection／Topic 的兩個分支；既有容器仍可由 tenant-owned ID、符合門檻的 active topic slug、或 exact duplicate 連結。
- 本機契約測試 19/19、JavaScript syntax checks、`git diff --check` 與 `npm run build` 均通過；未連線或寫入 Supabase、Vault、排程或 main。

## Stage O 受控資料庫部署

- Owner 於 2026-09-04 明確授權後，對 Supabase project `dcyjictvatixbflfrsfg` 執行單一 migration `stage_o_stop_auto_container_creation`（版本 `20260903170013`）。
- 部署前確認 live function 是 Stage M 版本，仍含 Collection／Topic auto-insert；相關 schema 欄位完整，且沒有 processing 中的 preprocess workflow。
- 部署後確認貼文／Collection／Topic 與既有自動建立容器計數均未變動（376／55／60／33／60）；function 已無兩種 auto-insert，保留 `classification_suggestions`、analysis、Vault sync、review request、action plan。
- Function 維持 `SECURITY DEFINER`、`search_path=public, pg_temp`，且 execute 仍只授權 `service_role`；不改動 Vault、排程、既有分類或舊資料。
- Supabase security advisor 的 `collection_posts`／`collection_collections` RLS disabled 錯誤為部署前已存在且部署後未新增；此風險未納入本次授權範圍，需獨立評估，禁止直接修補。

## Stage P RLS 部署草稿

- Owner 授權僅建立隔離分支內的 RLS deployment source、前端 tenant filter 與契約測試；未套用 Stage P、未建立測試帳號、未修改 Supabase。
- 新增 `stage_p_collection_rls_hardening.sql`：在同一交易內預檢跨 tenant link、限制 grants 至 authenticated/service_role、建立 owner-only RLS policy，並以 `(collection_id, user_id)` composite FK 阻擋跨 tenant Collection 指向。
- FK 的 `ON DELETE SET NULL` 明確只作用於 nullable `collection_id`；不會把 `collection_posts.user_id` 設為 NULL。
- Browser 端貼文／Collection 直接 mutation 均加入 `user_id` filter；reference schema、README 與契約測試同步更新。
- 本機 23 項契約／回歸測試、JavaScript syntax check、`git diff --check` 與 `npm run build` 通過；Docker 驗證依 Owner 既有決定跳過。
