# 2026-09-06

## Legacy Topic 封存相容修正

- 正式資料庫資料遷移 preflight 發現 Stage O 的
  `collection_topics_agent_origin_status` constraint 將既有
  `agent_auto` Topic 限制為 `active`，與已確認的「保留稽核軌跡但封存 legacy
  Topic」目標衝突。
- 新增 `database/deployments/stage_r_archive_legacy_agent_topics.sql`：只允許
  既有 `agent_auto` Topic 轉為 `archived`；不改變 Stage O trigger 對未來
  agent_auto 建立／重新標記的拒絕行為。
- 正式資料交易首次因該 constraint 中止並經獨立 read-only query 確認完整
  rollback：沒有新增 Project、沒有重掛 Collection／貼文、沒有更新 source match。
- Stage R 套用後，以單一交易完成：建立 6 個 Project、11 個 user-owned Topic；
  57 個 source match 移至精確的 Project × Domain Topic 並記為 user accepted；
  60 個 legacy Topic 封存。3 個未命中 Topic 的 source match 保留在封存資料，不猜測重掛。
- 183 篇貼文回連至既有 Owner Collection，`法律科技` 的 1 篇回到 inbox；33 個
  Hermes Collection 保留但已無貼文，沒有刪除任何 Collection，也沒有觸碰 Vault 實體檔。
- Postflight：378 Posts、89 inbox、6 active Projects、11 active user Topics、
  60 archived legacy Topics、57 accepted migrated matches、3 unmapped legacy matches。

## Mac PM2 部署防呆

- 移除 `pm2/ecosystem.config.cjs` 對 `~/Projects/...` 的硬編碼假設；改成必填
  `MEDIA_PLATFORM_PROJECT_ROOT`，並在啟動前驗證 API、Capture Worker 與
  `server/.env` 都位於該 checkout。這會 fail-fast，避免 PM2 服務到舊副本。
- `pm2/README.md` 已改為 Mac 部署順序：指定實際 checkout、fast-forward pull、
  安裝鎖定依賴、build，最後以 `startOrRestart --update-env` 啟動既有兩個程序。

## 推送 main（Owner 授權）

- 已將包含 content-first Vault、legacy Topic 收斂、Stage R 與 Mac PM2 防呆的
  `main` 推送至 `origin/main`。Mac 主機必須先 pull 到此版本，才可依更新後的
  PM2 文件重啟既有 API 與 Capture Worker。

## 內容整理收尾規劃（待確認）

- Owner 要求先完整規劃，確認後逐項開工；本次只新增
  `.hermes/plans/2026-09-06-content-organization-completion.md` 與此紀錄。
- 計畫涵蓋保護清單／關聯盤點、UI 收斂、未分類與 Topic 覆核、核准清單套用、
  Mac 上線、Vault 搬移提案、小批次搬移及最終驗收。
- 明列尚未確認的多重收藏／scope 引用、Mac 執行版本、Vault 手寫內容／連結與
  檔案碰撞；先穩定分類再搬移，避免重複搬檔。未實作任何階段，未改正式資料或排程。

## 內容整理第 0 階段完成

- 在隔離分支 `codex/content-org-phase0` 做正式資料庫唯讀盤點，結果固定於
  `docs/content_organization_baseline_2026-09-06.md`。
- 確定 22 個 Owner Collection ID 為保護清單；289 篇貼文歸屬於此範圍，89 篇維持 inbox。
- 33 個 Hermes legacy Collection 的主要貼文、post map、Topic scope 均為 0，但 183 個
  workflow 的 `context.folder_persistence` 保留歷史分類。這些 history 不在 UI 收斂時改寫。
- 未修改正式資料庫、Vault、排程或既有貼文／Topic／scope；第 1 階段可只調整 UI 顯示與選擇器。

## 內容整理第 1 階段完成

- 在隔離分支 `codex/content-org-phase0` 收斂 Topic 與 Collection 的日常 UI；沒有寫入正式資料庫、Vault 或排程。
- Topic 頁改為只顯示 active user Topic，按既有 Project 分組；60 個封存 Topic 另由歷史入口查閱。同步修正 `authenticatedFetch` 尚未 await 就交給 `responseData` 的載入錯誤。
- 收藏夾主列保留所有 22 個 Owner Collection；33 個 `Hermes 自動建立` legacy Collection 改為展開後的唯讀歷史入口，禁止拖放、改名、刪除與從彈窗移除貼文。
- 新增 `test/script/content_organization_ui_contract.test.js`；Node 全套測試 159/159 通過，`npm run build` 完成。build 只有既有 caniuse-lite 與 bundle size 非阻斷警告。

## 內容整理第 2 階段唯讀提案完成

- 以正式資料庫的 89 篇 inbox 貼文為範圍，僅讀取內容、既有摘要與 tags；未將 platform 納入分類規則。
- 新增 `docs/inbox_organization_proposal_2026-09-06.csv`：52 篇中／高信心的既有 Owner Collection 候選，37 篇低信心或未命中列維持 inbox；每列都有 source ID、目標 ID、內容概念證據、信心與必須 Owner 核准旗標。
- 新增對應的 `docs/inbox_organization_proposal_2026-09-06.md`，記錄判斷邊界、各分類數量與第 3 階段的重查／跳過條件。
- 未更新任何 `collection_id`、Topic、Project、scope、搜尋投影、workflow JSON 或 Vault；第 3 階段只能套用 Owner 明確核准的 source ID。

## 內容整理第 3 階段第 1 批完成

- Owner 核准先套用 11 筆：投資 3、狗狗 5、Codex 3。套用前確認 11/11 均仍為 inbox、同一 Owner、目標 Collection 存在，且搜尋投影完整且一致。
- 以一筆原子交易同步更新 `collection_posts.collection_id` 與 `collection_post_search_documents.collection_id`；交易內要求兩者各更新 11 列，任一數量不符即 rollback。
- 第一次交易因手寫重複 move 清單時有一筆目標 UUID 打字錯誤，被 tenant-aware foreign key 阻擋；交易已完整 rollback，重查確認 11/11 仍為 inbox、投影仍一致後，才以單一共用 move 清單重送。
- Postflight：11/11 貼文與搜尋投影皆在正確目標；總貼文仍 378、inbox 89→78、全庫搜尋投影不一致 0；目標計數為投資 15、狗狗 6、codex 3。
- 批次 journal：`docs/content_organization_batch_01_2026-09-06.md`。未變更 Topic、Project、scope、workflow JSON、Vault 或排程。

## 內容整理第 3 階段第 2 批完成

- Owner 核准套用 11 筆：工程師開發優化 6、UI skill 3、好skill 2。preflight 11/11 仍為 inbox、同一 Owner、目標與搜尋投影均完整一致。
- 使用第 1 批修正後的單一共用 JSON move mapping；交易內同步更新貼文與搜尋投影，各自必須更新 11 列，否則整筆 rollback。
- Postflight：11/11 貼文與搜尋投影皆在正確目標；總貼文仍 378、inbox 78→67、全庫搜尋投影不一致 0；目標計數為工程師開發優化 20、UI skill 4、好skill 4。
- 批次 journal：`docs/content_organization_batch_02_2026-09-06.md`。未變更 Topic、Project、scope、workflow JSON、Vault 或排程。
