# 2026-07-29 開發日誌

## POC E2E：第 5 筆 AI Code Review 來源

- 執行 outbox：`ca45f652-9f38-4f80-9cae-579a1ad45040`。
- MiniMax Route Agent 正確分類為 `apply_poc`；Tavily 因未設定 `TAVILY_API_KEY` 降級，沒有中斷流程。
- 修正 Project Auditor：將 JavaScript/TypeScript 的具體 `TODO/FIXME` 實作標記轉成 `missing_feature`。本專案因此辨識出 `threadsCrawler.js` 與 `socialApiService.js` 兩項真實缺口。
- 模型兩次生成的 POC 都嘗試使用網路能力，靜態檢查在 container 啟動前阻擋，並已將失敗結果寫入 `collection_post_analysis.insights`。
- 新增單次修復後的 deterministic fallback：只含 Node 標準 `assert`、固定規則與固定範例，結果會標示生成方式與 fallback 原因。
- 成功結果：run_id `66673c21-fd9f-423a-a8ad-d7bed8420d9a`，Docker sandbox 成功，輸出 `POC passed: detected 2 deterministic findings`，已寫入 Supabase。
- 驗證：13 個相關 Node 測試全數通過；ESLint 通過。
- outbox 保持 `pending`：同一來源尚有 `quick_rewrite`／研究路由，POC 成功不能直接標為 `sent`。
- 後續已在 `server/.env` 設定並實測 `TAVILY_API_KEY`：同一來源的 enrichment 查詢於 4.3 秒完成，結果寫入本機 `sandbox/.enrichment_cache.json` 快取；未修改 Supabase 正式資料。

## Outbox 多路由完成狀態

- 新增 `server/services/outboxRouteStateService.js`：在既有 `collection_capture_outbox.payload` JSONB 建立 `agent_routes` schema version 1，不新增資料表或 migration。
- 每條路由獨立保存 `pending`／`in_progress`／`completed`／`failed`／`skipped` 與 outcome；只有所有路由進入 terminal 狀態，outbox 才會變為 `sent` 或 `failed`。
- 所有 payload 寫入以 `updated_at` 作樂觀鎖；併發覆寫會明確失敗並要求重讀。
- 真實驗證第 5 筆：`apply_poc=completed`（重用 run_id `66673c21-fd9f-423a-a8ad-d7bed8420d9a`）、`quick_rewrite` 與 `translate_localize=pending`，outbox 正確保持 `pending`。
- 驗證：21 個相關 Node 測試全數通過；ESLint 通過。
- 風險追蹤：Supabase 官方已停止支援 Node 20；後端與 POC Node runner 仍為 Node 20，下一項基礎建設工作應升級至 Node 22。

## Content Studio 路由閉環

- 生產環境 preflight：`content_assets`、`content_revisions`、`content_evidence_links` 的 REST schema cache 找不到資料表，不能保存 content route 草稿。
- 已新增 `stage_c_content_studio.sql`：建立三張 tenant-aware 表、RLS／grants、revision idempotency 與 service-role-only `store_content_draft` transaction RPC。
- 已新增 `contentRouteService.js` 與 `agent:content` CLI，會將每個 pending `quick_rewrite`／`translate_localize` route 的 draft、revision、source/POC evidence 寫入後，再更新 outbox route state。
- 驗證：16 個相關 Node 測試全數通過；ESLint 通過。production DDL 尚未執行，等待使用者在 SQL Editor 套用 deployment。

## 任務：將 Docker Desktop 資料從 C 槽遷移至 G 槽

### 安全盤點

- Docker Desktop 使用 WSL 2 backend。
- 原始 VHDX：`C:\Users\bgkon\AppData\Local\Docker\wsl\disk\docker_data.vhdx`。
- 原始檔大小：35,977,691,136 bytes（33.507 GB）。
- C 槽剩餘 56.61 GB；G 槽剩餘 579.30 GB。
- 正式目標：`G:\DockerDesktopData`。
- 備份目標：`G:\DockerDesktopBackup\2026-07-29\docker_data.vhdx`。
- 備份前已確認 Docker processes 不存在，VHDX 可用 exclusive read/write 開啟。

### 備份事故與修正

- 第一次 `File.Copy` 在 120 秒工具 timeout 後被終止。
- 部分備份的 byte length 雖與來源一致，但 SHA-256 不同，因此判定無效。
- 僅刪除該次建立且已確認無效的單一備份檔。
- 改用 `robocopy /J /R:1 /W:1` 重新複製；33.506 GB 全數完成，failed=0、mismatch=0，耗時 6 分 54 秒。

### 完整性驗證

- Source SHA-256：`B46A6480A1A3B197D66244B2DA8E6D92E1DD07DF938F9A67FE3A94FB9C4B2C09`。
- Backup SHA-256：`B46A6480A1A3B197D66244B2DA8E6D92E1DD07DF938F9A67FE3A94FB9C4B2C09`。
- 結果：完全一致，備份可作遷移失敗時的復原來源。

### 目前狀態

- 使用者已透過官方 GUI 將 Disk image location 設為 `G:\DockerDesktopData\DockerDesktopWSL`。
- Docker Desktop 使用自己的 `robocopy /MOV /J` 完成搬移；C 槽舊 VHDX 已由 Docker 移除，G 槽新 VHDX 為 33.507 GB。
- 搬移後 Docker client/server 27.1.1 均可正常回應。
- 已下載 `node:20-alpine`，digest：`sha256:fb4cd12c85ee03686f6af5362a0b0d56d50c58a04632e6c0fb8363f609372293`。
- 已下載 `python:3.12-alpine`，digest：`sha256:6d43704baacd1bfbe7c295d7f13079d5d8104ed33568873133f8fc69980419df`。
- `node scripts/agent-sdk/poc-smoke.js` 真實驗證通過：JavaScript success（4140 ms）、Python success（4527 ms）。
- 搬移前完整備份仍保留在 `G:\DockerDesktopBackup\2026-07-29\docker_data.vhdx`，未刪除。
- 下一步為選擇一筆真實 outbox item，執行 MiniMax／Tavily／Docker／Supabase 完整 E2E；因涉及 API 額度與正式資料寫入，尚未執行。

## Content Studio 實跑修復

- 使用者已在 Supabase 執行 `stage_c_content_studio.sql`；服務角色預檢確認三張 Stage C 資料表與 `store_content_draft` RPC 都可存取。
- 第一次實跑內容路由時，PostgreSQL 回報 `column reference "revision_number" is ambiguous`；交易失敗，未寫入半筆草稿資料。
- 已修正原始 Stage C migration，並新增 `stage_c_content_studio_hotfix_001.sql`。hotfix 只替換 RPC，將 PL/pgSQL 內所有同名資料表欄位以 alias 限定，保留 RLS、權限與既有資料。
- 第二次實跑發現證據鏈的 `ON CONFLICT (content_asset_id, ...)` 也會受回傳欄位衝突影響；交易已回滾。為保持已部署 migration 不可變，新增 `stage_c_content_studio_hotfix_002.sql`，改用 `ON CONFLICT ON CONSTRAINT content_evidence_links_unique`。
- 使用者執行 Hotfix 002 後，已重設先前失敗的 `quick_rewrite` 路由並重跑真實 E2E。`quick_rewrite` 與 `translate_localize` 均完成，outbox `ca45f652-9f38-4f80-9cae-579a1ad45040` 已成為 `sent`。
- 驗證：建立 `x_thread` 與 `linkedin_post` 各一份 `draft` 資產、各一個 AI revision（revision 1），每份皆含原始來源與已驗證 POC run `66673c21-fd9f-423a-a8ad-d7bed8420d9a` 的證據鏈。
- 使用者澄清此為原作者的原始貼文，不需要改寫。後續將內容路由視為可選草稿能力，不可當成預設交付或發布流程；既有草稿維持 draft，未做任何修改或刪除。
- 使用者指出只收到口頭完成報告、沒有可見成果。後續 E2E 驗收必須先提供前端／報告／可開啟資料入口，再宣稱交付完成。
- 已在貼文詳情頁補上 POC 驗證結果面板：從既有 `collection_post_analysis.insights` 讀取最新成功 POC，顯示驗證目標、執行結果、耗時、stdout 與 fallback 限制，不改動原始貼文或資料庫資料。
- 原始 `todo/2026-07-24_接續實作POC生成與沙盒執行.md` 的第 3、4 項已同步為完成：真實 Supabase 寫回、Docker sandbox 與前端可見結果皆已驗證。
- 已將使用者對 `threadsCrawler.js TODO` 與 POC 工作流自動化程度的提問與說明記入 `questions.md`。
